getwd()
str(d)
cbind(as.numeric(d$stratum == "open")[d$status == 1], (d$sim)[d$status == 1]))
cbind(as.numeric(d$stratum == "open")[d$status == 1], (d$sim)[d$status == 1])
str(cbind(as.numeric(d$stratum == "open")[d$status == 1], (d$sim)[d$status == 1]))
getwd()
str(stanData)
